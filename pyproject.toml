[project]
name = "skill-router"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ninja Coder", email = "2924289+ninjacoder13@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "typesafe-sdk>=0.6.0",
]

[project.scripts]
skill-router = "skill_router:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
