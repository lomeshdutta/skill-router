# Get a key from https://console.typesafe.ai (Jev early access). Never commit the real .env.
TYPESAFE_API_KEY=
# Optional switches read by the hook:
# SKILL_ROUTER_MOCK=1      force the offline keyword mock even if a key is set
# SKILL_ROUTER_DISABLE=1   turn the hook off without editing settings
# SKILL_ROUTER_QUIET=1     hide the one-line suggestion shown to you in the transcript
